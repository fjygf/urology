<view class="section">
  <view class="selectbox">
  <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
    <view class="picker">
      当前选择：{{array[index]}}
    </view>
  </picker>  
  </view>
  <view class="buttom">
  <van-button type="primary" bindtap="create">按钮</van-button>
  </view>
</view>